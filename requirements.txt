# =============================================================================
# Pravara Basin Morphometry — Python Dependencies
# Install: pip install -r requirements.txt
# For Google Colab:  !pip install -r requirements.txt --quiet
# Tested on Python 3.9, 3.10, 3.11
# =============================================================================

# ── Core GIS / Raster ─────────────────────────────────────────────────────────
geopandas>=0.13.0
rasterio>=1.3.0
shapely>=2.0.0
fiona>=1.9.0
pyproj>=3.4.0
gdal>=3.4.0            # install via conda or apt-get for system GDAL bindings

# ── Numerical / Scientific ────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
plotly>=5.14.0
seaborn>=0.12.0
kaleido>=0.2.1          # for Plotly static image export

# ── Machine Learning / Statistics ─────────────────────────────────────────────
scikit-learn>=1.2.0
statsmodels>=0.14.0

# ── Report Generation ─────────────────────────────────────────────────────────
reportlab>=4.0.0
jinja2>=3.1.0
Pillow>=9.5.0           # image handling in reports
weasyprint>=59.0        # optional: HTML-to-PDF alternative

# ── Interpolation / Spatial Ops ───────────────────────────────────────────────
scipy>=1.10.0           # already listed, includes interpolation
rtree>=1.0.0            # spatial index for geopandas
xarray>=2023.1.0        # optional: for netCDF/gridded data

# ── Excel Export ──────────────────────────────────────────────────────────────
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0            # progress bars
tabulate>=0.9.0         # table formatting in reports
colorama>=0.4.6         # coloured terminal output

# ── Jupyter / Colab ────────────────────────────────────────────────────────────
ipywidgets>=8.0.0       # interactive widgets
IPython>=8.0.0

# =============================================================================
# Google Colab Installation Notes:
# Most of the above are pre-installed in Colab. The following may need manual
# installation in a Colab notebook:
#
#   !apt-get install -y gdal-bin libgdal-dev python3-gdal --quiet
#   !pip install geopandas rasterio fiona shapely pyproj --quiet
#   !pip install plotly kaleido reportlab weasyprint --quiet
# =============================================================================
